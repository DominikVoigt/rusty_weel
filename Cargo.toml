[workspace]
members = [
    "rusty_weel",
    "rusty_weel_macro",
    "http_helper"
]


[profile.release]
#trip = true # Strips symbols from the binary
opt-level = "z" #optimize for size
lto = true # Link time optimization
#panic = "abort"
codegen-units = 1
debug = 1
