[workspace]
members = [
    "weel_bin",
    "weel_lib",
    "weel_macro",
    "http_helper"
]
resolver = "2"
package.include = ["/resources"]

[profile.release]
strip = true # Strips symbols from the binary
opt-level = "z" #optimize for size
lto = false # Link time optimization
codegen-units = 1